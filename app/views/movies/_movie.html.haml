%tr
  %td= movie.title 
  %td= movie.rating if :view == :index
  %td= movie.release_date
  %td= link_to "More about #{movie.title}", movie_path(movie)
  %td= link_to "Edit", edit_movie_path(movie)
  %td= link_to 'Destroy', movie_path(movie), :method => :delete